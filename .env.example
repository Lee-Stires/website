# Umami website ID for https://analytics.phxlab.io/script.js. Leave unset in
# local dev — the tracking script silently no-ops without it. Set the real
# per-site ID as an environment variable in the Vercel dashboard so it's
# available during the build.
PUBLIC_ANALYTICS_WEBSITE_ID=
